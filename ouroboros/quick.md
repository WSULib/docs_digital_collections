Much of this should exist elsewhere, but quick place for saving

* When restarting Tomcat, need to then resetart Ouroboros
    * example is `bitStream` that will not function until both are restarted
